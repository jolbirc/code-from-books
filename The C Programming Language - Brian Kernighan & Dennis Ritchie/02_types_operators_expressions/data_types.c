#include <stdio.h>

char c;             // a single byte, capable of holding one character in the local character set
int x;              // an integer, typically refelcting the natural size of integers on host machine
float f;            // single precision floating point
double d;           // double precision floating point
short int sh;       // 'int' can be omitted from short and long
long int longer;   