#include <stdio.h>

int main(void)
{
    // specifies that its value will not be changed
    const double e = 2.71828182845909;
    const char msg[] = "warning :";

    // can also be used with array arguments, to indicate that the function does not change that array
    int strlen(const char[]);

    return 0;
}


